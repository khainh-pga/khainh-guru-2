org: ${file(../../../default.yml):org}
app: ${file(../../../default.yml):app}
frameworkVersion: ${file(../../../default.yml):frameworkVersion}

service: glue-etl

custom: ${file(../../../default.yml):custom}

provider:
  name: aws
  runtime: python3.8
  region: 'ap-southeast-1'
  # stackName: ${self:service.name}-${sls:stage}-r
  stackName: glue-etl-${sls:stage}-r
  environment:
    STATE: ${sls:stage}

resources:
  # - ${file(./resources/user-table.yml)}
  # - ${file(./resources/product-table.yml)}
  - ${file(./resources/datalake.yml)}
  - ${file(./resources/glue.yml)}
